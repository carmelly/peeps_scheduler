#TODO: get rid of this and pass things around properly
# for now it is useful for testing
class Globals:
	date_format = "%Y-%m-%d %H:%M"
	datestr_format = "%B %d - %I%p"
	data_folder = "2025-03-08--04-05"
	verbosity = 1
	max_events = 7  # We can't compute permutations for more events than this at a time
